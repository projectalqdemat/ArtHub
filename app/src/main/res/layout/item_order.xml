<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- Order count -->
    <TextView
        android:id="@+id/order_count_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="1"
        android:textSize="18sp"
        android:layout_alignParentStart="true"/>

    <!-- Status indicators -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:orientation="horizontal">

        <!-- Circle indicators for status -->
        <View
            android:id="@+id/status_indicator_1"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:layout_marginEnd="4dp"
            android:background="@drawable/circle_gray"/>

        <View
            android:id="@+id/status_indicator_2"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:layout_marginEnd="4dp"
            android:background="@drawable/circle_green"/>

        <View
            android:id="@+id/status_indicator_3"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:background="@drawable/circle_red"/>
    </LinearLayout>

    <!-- Cancel button -->
    <Button
        android:id="@+id/cancel_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancel"
        android:layout_below="@id/order_count_text_view"
        android:layout_alignParentEnd="true"/>
</RelativeLayout>
